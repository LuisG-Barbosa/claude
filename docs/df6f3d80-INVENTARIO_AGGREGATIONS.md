# Inventario de nodos Aggregation del ORIGINAL (referencia)

Para cada Aggregation: sus Element hijos, marcando cuál tiene función de agregación.


## Aggregation 3
  - PostingDate: AGREGADA (defaultAggregation=MIN, isAggregated=True)
  - Product: agrupación (group by)
  - Plant: agrupación (group by)
  - PostingMonth: agrupación (group by)

## Aggregation 4
  - Product: agrupación (group by)
  - PostingDate: AGREGADA (defaultAggregation=MAX, isAggregated=True)
  - Plant: agrupación (group by)
  - PostingMonth: agrupación (group by)

## Aggregation 8
  - PostingDate: agrupación (group by)
  - GoodsMovementType: agrupación (group by)
  - Product: agrupación (group by)
  - Calculated_1: AGREGADA (defaultAggregation=SUM, isAggregated=True)
  - Plant: agrupación (group by)
  - PostingMonth: agrupación (group by)

## Aggregation 6
  - PostingDate: agrupación (group by)
  - GoodsMovementType: agrupación (group by)
  - Product: agrupación (group by)
  - Calculated_1: AGREGADA (defaultAggregation=SUM, isAggregated=True)
  - Plant: agrupación (group by)
  - PostingMonth: agrupación (group by)

## Aggregation 2
  - PostingDate: AGREGADA (defaultAggregation=NONE, isAggregated=False)
  - Product: agrupación (group by)
  - Calculated_1: AGREGADA (defaultAggregation=SUM, isAggregated=True)
  - Plant: agrupación (group by)
  - PostingMonth: agrupación (group by)

## Aggregation 1
  - Product: agrupación (group by)
  - PostingDate: AGREGADA (defaultAggregation=NONE, isAggregated=False)
  - Calculated_1: AGREGADA (defaultAggregation=SUM, isAggregated=True)
  - Plant: agrupación (group by)
  - PostingMonth: agrupación (group by)

## Aggregation 7
  - Product: agrupación (group by)
  - Plant: agrupación (group by)
  - Calculated_1: AGREGADA (defaultAggregation=SUM, isAggregated=True)
  - PostingDate: AGREGADA (defaultAggregation=MIN, isAggregated=True)

## Aggregation 9
  - PostingDate: agrupación (group by)
  - GoodsMovementType: agrupación (group by)
  - Product: agrupación (group by)
  - Plant: agrupación (group by)
  - Calculated_1: AGREGADA (defaultAggregation=SUM, isAggregated=True)

## 100and200
  - Calculated_1: AGREGADA (defaultAggregation=MAX, isAggregated=True)
  - Calculated_1: agrupación (group by)
  - Calculated_1: agrupación (group by)
  - PostingMonth: agrupación (group by)

## Aggregation 5
  - Calculated_1: AGREGADA (defaultAggregation=MAX, isAggregated=True)
  - Calculated_1: agrupación (group by)
  - Calculated_1: agrupación (group by)
  - PostingMonth: agrupación (group by)